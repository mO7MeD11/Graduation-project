name: graduationproject3
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # المكتبة دي اللي هتصلح الاتجاهات
    sdk: flutter
  cupertino_icons: ^1.0.8
  gap: ^3.0.1
  dio: ^5.9.0
  flutter_svg: ^2.2.3
  pin_code_fields: ^8.0.1
  flutter_bloc: ^8.1.6
  google_maps_flutter: ^2.6.1
  image_picker: ^1.1.2
  geolocator: ^13.0.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  shared_preferences: ^2.2.3
  google_fonts: ^6.2.1
  get_it: ^7.7.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
